// we use index.js to import all componets in this file and then export them together so that we do not have to import commands again and again
// better way for exporting Components

import InputBox from "./InputBox";

export {InputBox}